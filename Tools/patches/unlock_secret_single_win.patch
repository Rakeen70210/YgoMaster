--- a/YgoMasterServer/Acts/Act_Solo.cs
+++ b/YgoMasterServer/Acts/Act_Solo.cs
@@ -736,9 +736,16 @@
                 }
             }
             Dictionary<string, object> unlockedPackData = null;
-            if (newStatus == ChapterStatus.COMPLETE)
+            object unlockSecretObj;
+            bool hasUnlockSecret = chapterData.TryGetValue("unlock_secret", out unlockSecretObj) && unlockSecretObj != null;
+            if (hasUnlockSecret &&
+                (newStatus == ChapterStatus.MYDECK_CLEAR || newStatus == ChapterStatus.RENTAL_CLEAR))
             {
-                object unlockSecretObj;
-                if (chapterData.TryGetValue("unlock_secret", out unlockSecretObj))// custom
+                newStatus = ChapterStatus.COMPLETE;
+            }
+            if (newStatus == ChapterStatus.COMPLETE && hasUnlockSecret)
+            {
                 {
                     List<int> unlockSecretIds = new List<int>();